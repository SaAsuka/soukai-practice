# 書き方の規約

- TypeScript strict。`any` を書かない
- 業務の計算は `src/lib` に置く。画面（`.tsx`）に計算を書かない
- 1つの関数は40行以内
- 例外を握りつぶさない
- 外部パッケージを足さない。標準ライブラリと、いま入っている依存だけで書く
- テストは `src/lib/__tests__` に置く。既存のテストは書き換えない
- コメントは「なぜそうしたか」だけ書く。何をしているかはコードで示す
